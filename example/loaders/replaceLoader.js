const {urlToRequest} = require( 'loader-utils')

module.exports = function (source) {
    // console.log('options', this.query.cacheDirectory)
    // console.log('source', source)
    return source
}
